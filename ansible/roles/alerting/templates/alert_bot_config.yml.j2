# Alert bot configuration
discord_token: "{{ discord_bot_token }}"
discord_channel_id: "{{ discord_channel_id }}"
alert_provider: "{{ alert_provider | default('discord') }}"
log_level: "{{ alert_log_level | default('INFO') }}"
