# Cloudflared - Cloudflare Tunnel Connector
# Provides secure public access without port forwarding

name: cloudflared
description: Cloudflare Tunnel for public access
category: core
icon: si-cloudflare
subdomain: null # Internal tunnel service

access:
  groups: [] # Internal service
  public: false

dashboard:
  exclude: true # Internal service

dependencies:
  - traefik
