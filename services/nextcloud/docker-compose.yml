volumes:
  db_volume:
    external: true
    name: nextcloud_db

services:
  db:
    image: mariadb
    container_name: nextcloud_db
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    networks:
      - proxy
    command: --transaction-isolation=READ-COMMITTED --binlog-format=ROW
    volumes:
      - db_volume:/var/lib/mysql
    environment:
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - NEXUS_ROOT_DIRECTORY=${NEXUS_ROOT_DIRECTORY}

  app:
    image: lscr.io/linuxserver/nextcloud:latest
    container_name: nextcloud
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M
    networks:
      - proxy
    ports:
      - 10080:80
    depends_on:
      - db
    volumes:
      - ${NEXUS_ROOT_DIRECTORY}/Data/nextcloud:/var/www/html
    environment:
      - PUID=1000
      - PGID=1000
      - VIRTUAL_HOST=nextcloud.${NEXUS_DOMAIN}
      - NEXTCLOUD_HOSTNAME=nextcloud.${NEXUS_DOMAIN}
      - OVERWRITEHOST=nextcloud.${NEXUS_DOMAIN}
      - OVERWRITEPROTOCOL=https
      - TRUSTED_PROXIES=traefik
      - TZ=${TZ}
      - NEXUS_ROOT_DIRECTORY=${NEXUS_ROOT_DIRECTORY}
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.routers.nextcloud.rule=Host(`nextcloud.${NEXUS_DOMAIN}`)"
      - "traefik.http.services.nextcloud.loadbalancer.server.port=80"
      - "traefik.http.routers.nextcloud.entrypoints=https"
      - "traefik.http.routers.nextcloud.tls=true"
      - "traefik.http.routers.nextcloud.tls.certresolver=certchallenge"
      - "traefik.http.routers.nextcloud.middlewares=authelia@docker,security-headers@docker"

networks:
  proxy:
    external: true
