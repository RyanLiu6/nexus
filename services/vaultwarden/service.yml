name: vaultwarden
description: Bitwarden-compatible password manager
icon: bitwarden
category: core
subdomain: vault

access:
  groups: [admins]
  public: false

dependencies:
  - traefik
  - tailscale-access
