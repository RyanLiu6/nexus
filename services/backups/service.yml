# Backups - Backrest
# Backup management with Backrest and restic

name: backups
description: Backup management with Backrest and restic
category: utils
icon: mdi-backup-restore
subdomain: backups

access:
  groups: [admins]
  public: false

dependencies:
  - traefik
  - tailscale-access
