# Dashboard - Homepage
# Central homepage with links to all services

name: dashboard
description: Service dashboard and homepage
category: core
icon: si-homeassistant
subdomain: nexus

access:
  groups: [admins, members, finance]
  public: false

dashboard:
  exclude: true # Don't show dashboard on itself

dependencies:
  - traefik
  - tailscale-access
